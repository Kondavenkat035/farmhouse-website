{
  "name": "farmhouse-website",
  "version": "1.0.0",
  "description": "Farmhouse booking website",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "build": "rm -rf build && mkdir build && cp -r views public app.js package.json build/",
    "test": "jest --coverage"
  },
  "keywords": [
    "nodejs",
    "express",
    "farmhouse",
    "booking"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "ejs": "^3.1.9"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
